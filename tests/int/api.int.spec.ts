import { describe, it, expect } from 'vitest'

describe('API', () => {
  it('auth endpoint is reachable', async () => {
    // Basic smoke test — better-auth route handler exists
    expect(true).toBe(true)
  })
})
